package main

import (
    "bytes"
    "flag"
    "fmt"
    "io/ioutil"
    "math/rand"
    "runtime"
    "strings"
    "sync"
    "sync/atomic"
    "time"
)

const {
    ADD_BUFFER_SIZE = 32
    ASK_BUFFER_SIZE = 8
}

func main() {

}
